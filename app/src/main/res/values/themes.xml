<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AppRFID" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.AppRFID" parent="Base.Theme.AppRFID" />

    <style name="Theme.AppRFID.MenuPrincipal" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/menu_background_blue</item>
        <item name="android:navigationBarColor">@color/menu_background_blue</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>