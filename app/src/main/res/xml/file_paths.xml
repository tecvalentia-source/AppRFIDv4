<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Todos los archivos bajo getFilesDir() (incluye subcarpeta RFID) -->
    <files-path name="internal_files" path="." />
    <!-- Reportes en getFilesDir()/RFID/ -->
    <files-path name="rfid_reports" path="RFID/" />

    <cache-path name="my_cache_exports" path="exports/" />

    <external-path name="my_external_files" path="." />
</paths>